np.cos,
np.sqrt,
np.log,
np.exp,
np.array(list)
mat.T
np.arange(a,b,c): range(a,b,c) for np.
np.linspace(a,b,c): gives c uniformly spaced entries in (a,b) both inclusive
vec4.reshape: reshape vectors row wise. 
np.zeros,
np.ones,
np.eye(a),
+-*/ on np arrays.
np.matmul : matrix multiplication as in linear algebra
np.linalg.solve(A,b)
np.linalg.inv(mat)
np.unique
np.random.rand
np.random.randn
np.mean
np.std
np.min
np.max
accessing multiple entries in numpy arrays and matrices
np.save
np.savez
np.load

